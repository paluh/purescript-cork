module Cork.Graphics.Canvas.ImageData
  ( module Types
  , module Mutable
  )
  where

import Cork.Graphics.Canvas.ImageData.Mutable (Mutable) as Mutable
import Cork.Graphics.Canvas.ImageData.Types (AnyImageData(..)) as Types

-- foreign import height ∷ ImageBitmap → Int
-- foreign import width ∷ ImageBitmap → Int
